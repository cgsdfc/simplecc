void main() {
  int iarr[10];
  int ivar;
  char cvar;

  while (cvar);
  while (cvar == cvar);
  while (ivar == cvar);


  while ((cvar));
  while (ivar + cvar);
  while (ivar);
  while (ivar == ivar);

}
