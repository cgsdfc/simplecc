void main() {
  int iarr[10];
  int ivar;
  char cvar;

  while (cvar) {
    printf("type error in while");
  }

  while (cvar == cvar) {
    printf("type error in while");
  }

  while (ivar == cvar) {
    printf("type error in while");
  }

  while (ivar) {
    printf("ok");
  }

  while (ivar == ivar) {
    printf("ok");
  }

}
